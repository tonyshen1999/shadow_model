from Account import Account, ShadowAccount

class Adjustment:
    def __init__(self, account, amount, adj_class, adj_collection):
        self.__account = Account
        self.__amount = amount
        self.__adj_class = adj_class
        self.__adj_collection = adj_collection
    